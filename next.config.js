/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com'],
        loader: 'default',
      },
      reactStrictMode: false
}

module.exports = nextConfig
